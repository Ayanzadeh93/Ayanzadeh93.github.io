{
  "name": "ayanzadeh.com static site",
  "ports": [
    {
      "name": "web server",
      "port": 8080
    }
  ],
  "terminals": [
    {
      "name": "web",
      "command": "python3 -m http.server 8080 --bind 0.0.0.0",
      "description": "Serves the static site over HTTP so ES modules load with correct MIME types. Open http://localhost:8080."
    }
  ]
}
