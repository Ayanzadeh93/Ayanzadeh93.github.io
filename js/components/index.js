/**
 * Entry point for the site's Web Components.
 *
 * Loaded with `<script type="module">`, so it is deferred by default and is
 * silently ignored by any engine without ES module support — the static markup
 * underneath stays fully usable in that case.
 */

import './cite-dialog.js';
import './pub-explorer.js';
